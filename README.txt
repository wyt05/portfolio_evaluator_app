Step 1: To install the dependencies, type: pip install -r requirements.txt
Step 2: To run the app, just type: streamlit run app.py on the console.

#Note:

Remember to press 'Enter' when typing in 'type in your name' textbox before clicking submit
as the field isn't a textbox but a tag box.

Same for portfolio